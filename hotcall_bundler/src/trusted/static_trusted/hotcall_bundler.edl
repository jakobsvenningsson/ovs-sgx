/* libhotcall_bundler.edl - Top EDL file. */

enclave {

    include "hotcall.h"

    untrusted {
        void ocall_assert([in, string] char *str);
    };

    trusted {
    	    public int ecall_start_poller([user_check] struct shared_memory_ctx *sm_ctx);
    };
};
