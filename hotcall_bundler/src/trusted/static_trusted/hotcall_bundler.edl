/* libhotcall_bundler.edl - Top EDL file. */

enclave {

    include "hotcall.h"

    untrusted {};

    trusted {
    	    public int
            ecall_start_poller([user_check] struct shared_memory_ctx *sm_ctx);
    };
};
